funcation halvesAreAlike(s){
    var a =s.slice(0,s.length /2)
    var b= s.slice(s.length/2, s.length)

    var vowels={
        a:true,
        e:true,
        i:true,
        o:true,
        u:true,
    }

    var aVowels=0;
        var bVowels=0;
 var loopingThroughA =true;
        for (var i = 0; i < s.length; i++) {
            if
            
        }
}